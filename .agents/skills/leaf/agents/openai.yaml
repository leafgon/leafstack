interface:
  display_name: "LEAF Programming"
  short_description: "Author LEAF graphs offline or persist directly"
  default_prompt: "Use $leaf to author LEAF graphs in local JSON files, validate them, and optionally persist via live APIs when requested."
